#include <bits/stdc++.h>

#define ull unsigned long long

using namespace std;

int main(void)
{
	int t;
	cin>>t;
	while(t--)
	{
		ull int L, R;
		cin>>L>>R;
		string binary = bitset<8>(n).to_string();
	}
}