#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main(void)
{
	int t;
	cin>>t;
	while(t--)
	{
		ll int n;
		cin>>n; 
		ll int a[n];
		ll int pf[n+1];
		memset(pf, 0, sizeof(pf));
		for(ll int i=0; i<n; i++)
			cin>>a[i];
		

	}
}